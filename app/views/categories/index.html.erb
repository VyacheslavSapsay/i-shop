<% set_page_title("Categories | I-Shop") %>
<div class="container">
  <h1><%= t ("general.categories") %></h1>
  <% @categories.each do |category| %>
    <%= link_to category.title, category_path(category) %>&nbsp;<span>(<%= category.products.count %>)</span>
    <br>
  <% end %>
</div>
