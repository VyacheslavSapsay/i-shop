<div class="product border border-primary">
  <strong> <%= link_to (fa_icon "cart-plus"), root_path, class: "pull-right btn btn-outline-primary" %><%= product.category.title %></strong>
  <br>
  <br>
  <h2> <p class="text-center"><%= link_to product.title, product_path(product) %></p></h2>
  <p class="text-center"><%= product.price %>₴</p>
  <%= image_tag product.images.first.url(:thumb) %>
  <% end %>
<% # link_to (fa_icon "shopping-cart", text: "buy"), root_path, class: "btn btn-outline-primary" %>
</div>
<br>
