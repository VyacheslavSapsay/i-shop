<div class="container">

<%= form_with(model: user, local: true) do |form| %>
  <% if user.errors.any? %>
    <div class="alert alert-danger">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this author from being saved:</h2>

      <ul>
        <% user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <%= form.text_field :email, type: "email", class: "form-control", id: "email", placeholder: "Enter email", autocomplete: "off" %>
  </div>

  <div class="form-group">
    <%= form.password_field :password, type: "password", class: "form-control", id: "password", placeholder: "Enter password" %>
  </div>

  <div class="form-group">
    <%= form.password_field :password_confirmation, type: "password", class: "form-control", id: "password", placeholder: "Confirm password"  %>
  </div>

<div class="text-center">
  <div class="form-group">
    <%= form.submit class: "btn btn-primary" %>
  </div>
  </div>
<% end %>
</div>
